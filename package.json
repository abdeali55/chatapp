{
  "name": "chatapp",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "scripts": {
    "build": "pnpm -r --no-include-workspace-root run build",
    "build:common": "pnpm --filter @chatapp/common build",
    "dev": "pnpm -r --no-include-workspace-root run dev",
    "lint": "pnpm -r --no-include-workspace-root run lint",
    "format": "pnpm -r --no-include-workspace-root run format",
    "test": "pnpm -r --no-include-workspace-root run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^12.3.4",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "@typescript-eslint/eslint-plugin": "^8.66.0",
    "@typescript-eslint/parser": "^8.66.0",
    "eslint": "^10.8.1",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
